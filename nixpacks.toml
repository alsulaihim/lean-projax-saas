[phases.setup]
nixPkgs = ['nodejs_20', 'openssl']

[phases.install]
cmds = ['npm install']

[phases.build]
cmds = [
  'npx prisma generate',
  'NEXTAUTH_SECRET=build-time-placeholder npm run build:prod'
]

[start]
cmd = 'npx prisma migrate deploy && npm run start:prod'
